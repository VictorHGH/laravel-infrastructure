.git/
.vscode/
.idea/
.DS_Store
*.log

# Infra local / plantilla (no necesario en prod)
docker-compose.yml
proyecto.txt
generar_proyecto_txt.sh
treecat.py

# (Opcional) docs
README.md

# (Opcional) archivos de repo
.dockerignore
.gitignore

# Datos locales (no subir)
mysql_dev_data/
mysql_data/

# Secretos (no subir)
mysql/.env
src/.env
src/.env.local
src/.env.production
src/.env.staging
src/.env.testing
src/auth.json

# Dependencias (en prod se regeneran en build con composer)
src/vendor/

# Frontend / caches (no subir)
src/node_modules/
src/.npm/
src/.yarn/
src/.pnpm-store/
src/.cache/
src/.vite/

# Laravel runtime (no sobreescribir en deploy)
src/bootstrap/cache/
src/storage/

# Test / tooling (no necesario en prod)
src/tests/
src/.phpunit.result.cache
src/phpunit.xml
